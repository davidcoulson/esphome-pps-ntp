# MUST FAIL validation: task_core on a single-core chip
esphome:
  name: ci-invalid
esp32:
  variant: esp32c3
  framework:
    type: esp-idf
external_components:
  - source:
      type: local
      path: ../../components
logger:
api:
wifi:
  ap: {}
uart:
  id: gnss_uart
  rx_pin: GPIO20
  tx_pin: GPIO21
  baud_rate: 9600
pps_ntp:
  uart_id: gnss_uart
  pps_pin: GPIO4
  task_core: 1
